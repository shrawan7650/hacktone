// const mongoose = require('mongoose');

// const heartRateSchema = new mongoose.Schema({
   
//     heart_rate: { type: Number, required: true }, // Heart rate in beats per minute
//     // patient_id: { type: String, required: false }, // Optional Patient ID
//     activity_status: { type: String, enum: ['resting', 'walking', 'running'], required: true } // Activity status
// },

// {
//     timestamps: true, // Adds createdAt and updatedAt fields automatically
  
// });


// const HeartRate = mongoose.model('HeartRate', heartRateSchema);
// module.exports = HeartRate;